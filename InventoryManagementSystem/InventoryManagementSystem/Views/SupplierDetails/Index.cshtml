@model IEnumerable<InventoryManagementSystem.Models.SupplierDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContactNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContactPersonName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Suppliers.SupplierEmail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContactNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContactPersonName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Suppliers.SupplierEmail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SupplierDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SupplierDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SupplierDetailID })
        </td>
    </tr>
}

</table>
